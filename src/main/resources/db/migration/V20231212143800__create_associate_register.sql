CREATE TABLE IF NOT EXISTS associate (
                                         id INT PRIMARY KEY,
                                         name VARCHAR(255) NOT NULL,
                                         email VARCHAR(255) NOT NULL
);
